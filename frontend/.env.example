# Empty value means same-origin calls, useful only if frontend and backend are served
# from the same domain/reverse proxy.
#
# For separate frontend/backend deployments, set this to the backend origin.
VITE_PRODUCT_TO_MCP_API_BASE_URL=https://api.your-domain.com

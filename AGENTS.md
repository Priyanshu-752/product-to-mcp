# Agent Guidance

Read `critical_prompt.md`, `context.md`, `instructions.md`, `structure.md`,
and the active plan before changing implementation. Keep the prototype easy to
run and inspect. Prefer direct contracts over premature abstractions. Never
put credentials in tracked artifacts. Validate both the API behavior and the
rendered frontend behavior for user-facing changes.

